<!DOCTYPE html>
<html lang="en">
  <head>
    <title>{{ title or "Component Example" }}</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <link rel="stylesheet" href="{{ '/src/css/app.css' | url }}?v={% version %}" />
    <link rel="stylesheet" href="{{ '/assets/css/component-preview.css' | url }}?v={% version %}" />
  </head>
  <body>
    <main aria-label="Component Example">
      <div class="component-preview-contents">
        {{ content | safe }}
      </div>
    </main>
  </body>
</html>
