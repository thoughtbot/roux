---
title: Tables
description: Tables present structured data that needs clear relationships between rows and columns.
permalink: /component-library/tables.html
---

{% from "partials/_component-preview.njk" import preview %}

{% set example_basic %}
  {% include 'partials/examples/tables/basic.html' %}
  {% endset %}

  {% set example_striped %}
  {% include 'partials/examples/tables/striped.html' %}
  {% endset %}

  {% set example_overflow %}
  {% include 'partials/examples/tables/overflow.html' %}
  {% endset %}
  
<h1>{{ title }}</h1>

{% if description %}
<p>{{ description }}</p>
{% endif %}

<h2>Considerations</h2>

<p>We use semantic HTML elements (<code>&lt;table&gt;</code>, <code>&lt;caption&gt;</code>, <code>&lt;thead&gt;</code>, <code>&lt;tbody&gt;</code>, <code>&lt;tfoot&gt;</code>) so assistive technologies can interpret the table correctly.</p>
<ul>
<li><strong>Caption:</strong> Provides an accessible title for the table.</li>
<li><strong>Headers (<code>&lt;th&gt;</code>):</strong> Define columns so screen readers can associate cells with their labels.</li>
<li><strong>Footer (<code>&lt;tfoot&gt;</code>):</strong> Summarizes or totals data, allowing users to quickly understand aggregates without scanning the entire table.</li>
</ul>
<p><strong>Variations:</strong></p>
<ol>
<li><a href="#example_basic"><strong>Basic Table</strong></a></li>
<li><a href="#example_striped"><strong>Striped Table</strong></a> (<code>.table--striped</code>): Alternating row backgrounds improve readability for large datasets.</li>
<li><a href="#example_overflow"><strong>Overflow Scroll</strong></a>: Wrapping a table in <code>.table-outer-wrapper</code> and <code>.table-inner-wrapper</code> allows tables to scroll horizontally on smaller viewports, ensuring content remains accessible without breaking the layout.</li>
</ol>
<p>Using these variations keeps tables readable, accessible, and responsive across devices while maintaining semantic HTML.</p>



<h2>Examples</h2>



<h3 id="example_basic">Basic Table</h3>
<div>
  {{ preview(title="Basic Table", code=example_basic,
  id="example_basic") }}
</div>

<h3 id="example_striped">Striped Table</h3>
<div>
  {{ preview(title="Striped Table", code=example_striped,
  id="example_striped") }}
</div>

<h3 id="example_overflow">Overflow Scroll</h3>
<div>
  {{ preview(title="Overflow Scroll", code=example_overflow,
  id="example_overflow") }}
</div>
